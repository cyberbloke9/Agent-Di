org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.caching=true
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# On a network that intercepts TLS (a corporate/proxy CA), Gradle's downloads
# fail with certificate errors. Don't hardcode a machine path here; instead add
# a Java truststore that includes the proxy CA and pass it per build. See the
# "Building behind a TLS-intercepting proxy" section in README.md.
